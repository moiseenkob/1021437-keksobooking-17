'use strict';

(function () {

  var itemFirstCards = function () {
    // var firstPin = document.querySelector('.map__pin:not(.map__pin--main)');
    // var cardElements = templateCard.cloneNode(true);

  };

  window.itemCards = {
    itemFirstCards: itemFirstCards
  };

})();
